{
  "name": "wikimedia-task",
  "version": "1.0.0",
  "description": "Technical interview task - an app to collect and store info from prospective donors.",
  "main": "index.js",
  "scripts": {
    "start": "nodemon server -e html,js,scss --ignore public --ignore client",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gary Hagen",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "mysql": "^2.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
